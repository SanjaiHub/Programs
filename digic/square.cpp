//wap in c to find square of given number
#include"stdio.h"
main(){
	int num;
	long square;
	printf("Enter your number:");
	scanf("%d",&num);
	square=(num*num);
	printf("given number square is:%ld",square);
}