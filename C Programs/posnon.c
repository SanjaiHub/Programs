//wap a program to check weather a given number is positie or non positive
#include<stdio.h>
int main()

{
    int num;
    printf("Enter the Number\n=");
    scanf("%d",&num);
    if(num<0)
    printf("Number is Non-Positive");
    else
    printf("Number is Positive ");
    return 0;

}

